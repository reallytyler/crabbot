# Crab Bot with Gateway Proxy Support
# Custom fork for local gateway proxy at localhost:7878

# Custom discord.py fork for gateway proxy
git+https://github.com/discord-py-community/discord.py@gateway-proxy

# Core dependencies
aiohttp>=3.8.0
async-timeout>=4.0.0

# Optional utilities (included for compatibility)
python-dotenv>=0.19.0
orjson>=3.8.0

# Development tools (optional)
setuptools>=65.5.0
wheel>=0.37.0
